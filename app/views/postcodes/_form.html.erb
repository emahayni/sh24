<%= form_with(model: @postcode, local: true) do |form| %>

<% if @postcode.errors.any? %>
  <div id="error_explanation">
    <h2>
      <%= pluralize(@postcode.errors.count, "error") %> prohibited this postcode from being saved:
    </h2>
    <ul>
      <% @postcode.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<p>
  <%= form.label :code %><br>
  <%= form.text_field :code %>
</p>

<p>
  <%= form.label :description %><br>
  <%= form.text_area :description %>
</p>

<p>
  <%= form.submit %>
</p>
<% end %>

<%= link_to 'Back', postcodes_path %>